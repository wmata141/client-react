import React from 'react';

const Comentarios = () => {
    return (
        <div>
            <h1>Circles</h1>
            <h4>
                ● Write a function named findCircleCollisions that takes an array of circles
                as parameter and computes a new array with the circles that are colliding. A
                collision is defined by a positive intersection area between two circles.
            </h4>
            <h4>
                ● Write a new function named findCollisions that takes an array of shapes,
                including circles. You should leave the shape collision math to future
                implementors.
            </h4>
        </div>
    )
}

export default Comentarios 